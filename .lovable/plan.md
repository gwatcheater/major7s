Regenerate `open-major7s-leaderboard.md` with the same data and layout, but render the two non-counting picks as plain text (no `~~strikethrough~~`). CUT/WD prefixes (e.g. `CUT·100`) are preserved. No app code changes.